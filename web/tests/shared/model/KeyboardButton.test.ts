import { KeyboardButton } from "../../../src/shared/model";
import { describe, test, expect } from "vitest";

describe("KeyboardButton", () => {
  test("export keypress", () => {
    const kb = new KeyboardButton(0);
  });
});
